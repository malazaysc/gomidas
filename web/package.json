{
  "name": "gomidas-web",
  "version": "0.1.0",
  "private": true,
  "description": "Gomidas WebView layer — pure logic unit tests (Vitest). See docs/TESTING.md.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
