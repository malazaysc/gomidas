{
  "name": "gomidas-app-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Gomidas browser build — Vite shell over the shared editor in web/. See docs/WEB_PORT.md.",
  "scripts": {
    "dev": "npm --prefix ../../web run build && vite",
    "build": "npm --prefix ../../web run build && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.7"
  }
}
