name: CI

on:
  push:
    branches: [main]
  pull_request:

# Cancel superseded runs on the same ref.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Fast, reliable gate: pure-logic unit tests for the WebView layer (Vitest).
  # Runs in seconds — no native toolchain, no JUCE/sfizz fetch. See docs/TESTING.md.
  web-tests:
    name: Web unit tests (Vitest)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/core
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: packages/core/package-lock.json
      - run: npm ci
      - run: npm test

  # Native audio-path smoke tests (sfizz SFZ load + render) via ctest. Heavier: this
  # configures with -DGOMIDAS_BUILD_TESTS=ON, which fetches JUCE 8 + builds sfizz.
  native-tests:
    name: Native smoke tests (ctest, macOS)
    runs-on: macos-latest
    # A docs- or web-shell-only change should not spend ~10 minutes building JUCE and sfizz.
    # web/ IS still native-relevant: CMake compiles it into the binary (GMD-31), so it is not
    # excluded here.
    steps:
      - uses: actions/checkout@v4
      # The web front-end is TypeScript and CMake compiles it during configure
      # (GMD-31, docs/WEB_PORT.md §1.1), so the native job needs Node too.
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: packages/core/package-lock.json
      - name: Install web deps (for tsc)
        working-directory: packages/core
        run: npm ci
      - name: Cache CMake FetchContent deps (JUCE, sfizz)
        uses: actions/cache@v4
        with:
          path: build/_deps
          key: deps-${{ runner.os }}-${{ hashFiles('CMakeLists.txt', 'cmake/**') }}
      - name: Configure
        run: cmake -B build -DCMAKE_BUILD_TYPE=Debug -DGOMIDAS_BUILD_TESTS=ON
      - name: Build test targets
        run: cmake --build build --target gomidas_tests
      - name: Run ctest
        run: ctest --test-dir build --output-on-failure
